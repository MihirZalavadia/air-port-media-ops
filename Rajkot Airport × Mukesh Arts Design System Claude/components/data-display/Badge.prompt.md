**Badge** — small mono tag. `solid` = off-white plate for use over photos (inventory `CODE · CATEGORY`); `outline` = hairline red pill for status labels on surfaces.

```jsx
<Badge>PKG-01 · Digital Packages</Badge>
<Badge variant="outline">Awaiting confirmation</Badge>
```
